<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="document">
  <select id="list" resultType="com.erp.dto.DocumentDto">
    select * from document
  </select>
  
  <select id="findByCategory" resultType="com.erp.dto.DocumentDto">
    select * from document where category_code = #{categoryCode}
  </select>
  
  <insert id="save" parameterType="com.erp.dto.DocumentDto">
    insert into document (document_title, document_content, document_status, document_createBy, document_createAt, category_code)
    values (#{documentTitle}, #{documentContent}, #{documentStatus}, #{documentCreateBy}, #{documentCreateAt}, #{category_code})
  </insert>
  
  <select id="detail" resultType="com.erp.dto.DocumentDto">
    select * from document where document_no = #{documentNo}
  </select>
  
  <update id="edit" parameterType="com.erp.dto.DocumentDto">
    update document set document_title = #{documentTitle}, document_content = #{documentContent}, 
      document_status = #{documentStatus}, document_updateAt = #{documentUpdateAt} where document_no = #{documentNo}
  </update>
  
  <delete id="delete" parameterType="int">
    delete from document where document_no = #{documentNo}
  </delete>
</mapper>
